{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "build:api": {
      "outputs": ["api/*.js"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
